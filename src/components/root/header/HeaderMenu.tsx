import { Box } from "@mui/material";

const HeaderMenu = () => {
  return (
    <Box sx={{ marginLeft: "-15px", marginRight: "10px", marginTop: "5px" }}>
      <img src="/src/assets/logo.png" width={32} height={32} />
    </Box>
  );
};

export default HeaderMenu;
